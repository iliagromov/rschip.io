//-select
mixin SelectBox(name, options)
    .select-box__current(tabindex="1")
        .select-box__value
            input.select-box__input(type="radio" id=`id_${name}` value=name name=name checked)
            p.select-box__input-text=name
        each option, index in options
            .select-box__value
                input.select-box__input(type="radio" id=`id_${name}_name_${option}_idx_${index}` value=option.name name=name )
                p.select-box__input-text #{option}
        .svg-iconArrowSelect.svg-iconArrowSelect-box
    ul.select-box__list
        each option, index in options
            li
                label.select-box__option(for=`id_${name}_name_${option}_idx_${index}` aria-hidden) #{option}
        


.selectBox 
    .selectBoxText Find out how many Horsepowers you can add to your Car
    .selectBoxContainer
        #brands.select 
            .select-box 
                .select-box__icon
                    .svg-iconArrowSelect.svg-iconArrowSelect-box
                .select-box__empty 
                    .svg-iconSelectEmpty.svg-iconSelectEmpty-box
        #models.select 
            .select-box 
                .select-box__icon
                    .svg-iconArrowSelect.svg-iconArrowSelect-box
        #Modification.select
            .select-box 
                .select-box__icon
                    .svg-iconArrowSelect.svg-iconArrowSelect-box
            
        .selectBoxSearch
            .svg-iconSearch.svg-iconSearch-box
    a.selectBoxLink.js-open-modal(href="javascript:void(0)" data-modal="modalCarCantfind") Can’t find my Car
